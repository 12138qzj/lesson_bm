const defaulState = {
    category: "",
    alpha: "",
    singerList: "",
    enterLoading: true,
    listOffset: 0,
}

export default (state = defaultStatus, action) => {

    switch (action.type) {
        // case :
        default: return state;
    }
}