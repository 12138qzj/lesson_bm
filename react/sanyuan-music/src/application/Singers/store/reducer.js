const defaulState = {
    category: "",
    alpha: "",
    singerList: "",
    enterLoading: true,
    listOffset: 0,
}

export default (state = defaulState, action) => {

    switch (action.type) {
        // case :
        default: return state;
    }
}